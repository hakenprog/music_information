<%= form_with(model: location, local: true) do |form| %>
  <% if location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
        <% location.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.collection_select(:country_id, Country.all, :id, :nombre) %>

  </div>

  <div class="field">
   <%= form.collection_select(:province_id, Province.all, :id, :nombre) %>

  </div>

  <div class="field">
    <%= form.collection_select(:city_id, City.all, :id, :nombre) %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
